/**
 * Заглушка экрана входа.
 * Форма + проверка токена — UI-1.
 */
export function LoginPage() {
  return (
    <main>
      <h1>Вход</h1>
      <p>Экран входа будет в UI-1 (base URL + Bearer-токен).</p>
    </main>
  )
}
