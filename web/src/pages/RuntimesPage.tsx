/**
 * Заглушка списка runtimes (полный экран — UI-6).
 * Маршрут нужен для навигации AppShell уже в UI-1.
 */
export function RuntimesPage() {
  return (
    <main>
      <h1>Runtimes</h1>
      <p>Список runtimes появится позже.</p>
    </main>
  )
}
