// Package store — порт хранилища (интерфейсы) для бизнес-логики.
//
// Сейчас пакет-заглушка (Phase 0.1): каталог входит в модуль.
// Узкие интерфейсы (BotRepository и др.) без импорта драйверов БД
// появятся в Phase 0.3. Реализация in-memory — в package memory (Phase 0.4).
package store
